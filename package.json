{
  "name": "marks-weather",
  "version": "1.0.0",
  "description": "Simple Weather PWA",
  "repository": "git@bitbucket.org:dmt0822/weather-pwa.git",
  "main": "server/app.js",
  "engines": {
    "node": "12.14.1",
    "npm": "6.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && npm start",
    "build": "cd client && npm run build",
    "build:dev": "cd client && npm run build:dev",
    "watch": "npm-run-all -p watch:client watch:server",
    "watch:server": "cd server && npm run watch",
    "watch:client": "cd client && npm start"
  },
  "author": "dmt",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "nodemon": "^1.19.4",
    "npm-run-all": "^4.1.5"
  }
}
